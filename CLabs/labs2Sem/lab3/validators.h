#ifndef VALIDATORS_H
#define VALIDATORS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include<ctype.h>

char* initString();
int getValidInput(int min, int max);
char getValidCharInteger(int min, int max);

#endif 